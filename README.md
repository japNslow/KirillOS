# KirillOS v0.2

Минималистичная 32-битная монолитная операционная система для архитектуры x86 (IA-32) с собственным загрузчиком, компилятором, hex-редактором, звуковым трекером и файловой системой.

---

## 🌟 Основные компоненты и возможности

- **Собственный загрузчик kboot**:
  - Двухстадийная загрузка: MBR (LBA 0) $\to$ kboot 32-bit Protected Mode.
  - Собственный VGA-терминал (`kboot> `).
  - Аппаратные прерывания клавиатуры через **IDT** и контроллер **PIC 8259A (IRQ1)**.
  - Встроенный загрузчик исполняемых файлов **ELF32**.
- **Файловая система KirillFS**:
  - Поддержка до 32 файлов размером до 2048 байт.
  - Постоянное хранение на жестком диске ATA (PIO LBA28).
  - Прозрачный откат в RAM при отсутствии диска.
- **Интерактивный Hex-редактор Kdhe**:
  - Двухпанельный просмотр (HEX + ASCII) с синхронной подсветкой.
  - Редактирование в HEX и ASCII (переключение по `Tab`).
  - Режим прямого редактирования секторов диска (`kdhe disk <LBA>`).
  - Полная поддержка стрелок, `Insert`, `Delete`, `PageUp/Down`, `Home/End`.
- **Музыкальный трекер и композитор Kmidi**:
  - Визуальная клавиатура фортепиано на 2 октавы с подсветкой нот.
  - Пошаговый секвенсор с графической шкалой длительностей.
  - Живая игра нотами с клавиатуры ПК (`Z..M`, `Q..U`).
  - Воспроизведение через **AC'97** (DMA Bus-Mastering) и **PC Speaker**.
  - Встроенные композиции: `tetris.kmidi`, `mario.kmidi`, `demo.kmidi`.
- **Подсистема приложений KHEX**:
  - Исполнение нативных `.bin` (x86) и байткод-программ `.khex`.
  - Системный API для программ: графика VGA, звук, неблокирующий ввод, таймер, ГПСЧ.
  - Игры в комплекте: `snake.bin` (змейка), `guess.bin` (угадай число), `matrix.bin` (цифровой дождь).
- **Текстовый редактор Kano (клон GNU nano)**:
  - Аутентичный интерфейс nano: верхний заголовок со статусом `[Modified]`, окно текста на 21 строку, строка уведомлений и двухстрочная панель горячих клавиш.
  - Аппаратный мигающий курсор VGA (CRTC) точно на позиции ввода.
  - Свободная навигация по тексту: стрелки, `Home`/`End`, `PageUp`/`PageDown`, `Ctrl+A`/`Ctrl+E`.
  - Вырезание и вставка строк через буфер: `Ctrl+K` (Cut Line) и `Ctrl+U` (Paste).
  - Быстрый поиск по тексту: `Ctrl+W` (Where Is) без учета регистра.
  - Переход к номеру строки (`Ctrl+\`), отображение позиции курсора и процента файла (`Ctrl+C`).
  - Вставка файлов из KirillFS прямо в текст (`Ctrl+R` / Read File).
  - Защита от потери данных: запрос на сохранение при выходе (`Ctrl+X` / `Ctrl+Q`).
- **Компилятор и линковщик KHEXD**:

  - Встроенный язык программирования **K** (`.k`).
  - Двухстадийная компиляция: `.k` $\to$ `.ko` (объектный файл) $\to$ `.khex` (исполняемый файл).
  - Безопасная виртуальная машина времени выполнения.
- **Аппаратные драйверы**:
  - VGA 80x25 (16 цветов, скроллинг, аппаратный курсор).
  - PS/2 клавиатура (Set 1, расширенные скан-коды `0xE0`, стрелки, спецклавиши).
  - ATA/IDE PIO LBA28.
  - Аудиоконтроллер **Intel AC'97** (PCI Class `0x040100`, Bus Mastering DMA, 48 kHz).
  - Системный таймер PIT 8254 и PC Speaker.

---

## 🛠️ Сборка и запуск

### Требования
- `i686-elf-gcc` и `i686-elf-ld` (кросс-компилятор x86)
- `nasm`
- `qemu-system-x86_64`
- `python` (версия 3.x)

### Сборка
Запустите командный скрипт:
```cmd
build.bat
```
Скрипт автоматически:
1. Соберёт загрузчик `kboot` (MBR + Stage 2).
2. Скомпилирует встроенные приложения (`apps/`).
3. Скомпилирует и слинкует ядро `kernel.bin`.
4. Сформирует загрузочный образ диска `kirillos.img` (64 МБ).
5. Запустит QEMU с эмуляцией жесткого диска и звука AC'97.

---

## 🎮 Команды оболочки KirillOS

- `run <file.bin | file.khex>` — запуск программы или игры.
- `khex list` — список всех доступных исполняемых программ.
- `khexd build <file.k>` — компиляция и линковка программы на языке K.
- `kdhe <file>` — открытие файла в hex-редакторе.
- `kdhe disk <LBA>` — просмотр сектора жесткого диска.
- `kmidi <file.kmidi>` — запуск визуального трекера музыки.
- `play <file.kmidi>` — воспроизведение мелодии из командной строки.
- `kano <file>` — текстовый редактор.
- `ls`, `cat`, `touch`, `write`, `rm`, `format`, `fsinfo` — команды файловой системы.
- `help`, `about`, `clear`, `echo`, `beep`, `tone`, `music` — системные утилиты.

---

## 📚 Техническая документация

В директории [`docs/`](docs/README.md) доступна подробная техническая документация по подсистеме разработки:
- [**docs/README.md**](docs/README.md) — Главная страница и архитектурный обзор инструментария разработки.
- [**docs/K_LANGUAGE.md**](docs/K_LANGUAGE.md) — Полное руководство по языку программирования K (.k): синтаксис, переменные, операторы, циклы, системные вызовы и примеры программ.
- [**docs/COMPILER_AND_LINKER.md**](docs/COMPILER_AND_LINKER.md) — Внутреннее устройство компилятора и линковщика KHEXD: двухстадийная сборка, формат `.ko` (KOBJ), линковка и команды CLI.
- [**docs/BYTECODE_AND_VM.md**](docs/BYTECODE_AND_VM.md) — Спецификация виртуальной машины KHEX VM: архитектура стека, таблица всех опкодов, структура заголовка `.khex` и пошаговый разбор ручной сборки Hello World в hex-редакторе `kdhe`.

---

## 🤖 Авторство и разработка


Код и архитектура операционной системы **KirillOS v0.2** (включая кастомный загрузчик **kboot**, MBR, аппаратные прерывания IRQ1/IDT, драйверы устройств, подсистему приложений **KHEX**, компилятор и линковщик **KHEXD**, hex-редактор **Kdhe**, звуковой трекер **Kmidi** и файловую систему **KirillFS**) разработаны и созданы с помощью ИИ-ассистента **Antigravity** (**Google DeepMind**) в совместной разработке с автором проекта.

